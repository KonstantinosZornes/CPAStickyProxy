# StickyProxy 粘滞代理上下文

StickyProxy 是一个轻量的账号级代理管理上下文：它登记可复用的上游代理，并按平台规则应用到 CPA 账号。粘滞平台按账号邮箱生成稳定连接身份；普通代理直接使用保存后的代理地址。

## 代理与平台

**代理**：一个有持久化名称、平台类型和基础地址的 HTTP、HTTPS 或 SOCKS5 代理配置，包含协议、主机、端口和可选的上游认证信息。
_避免_：节点、线路（除非明确讨论底层供应商资源）

**平台**：决定如何生成账号最终代理 URL 的供应商或通用适配规则。粘滞平台把账号身份编码到代理连接中；普通平台代理直接使用保存后的代理地址。目前支持 Decodo、DataImpulse、Resin、1024Proxy 和普通代理。
_避免_：供应商账号（平台是连接规则，不等同于上游登录账号）

**普通代理**：平台标识为 `generic` 的直接代理。插件把保存后的代理地址直接写给选中的账号，不在用户名或密码中加入账号 hash、session 或 SID；因此账号可能共享同一上游认证、会话或出口行为。
_避免_：直连、静态 IP、端口型粘滞代理、已保证可用或账号隔离的代理

**粘滞身份**：同一账号重复解析时使用的稳定 URL 身份。它只由规范化邮箱通过 SHA-256 确定性生成，重启、重新应用任意代理后都不会变化；上游供应商仍按自己的 TTL 决定出口 IP 实际保持期限。
_避免_：轮换代理、成功复用、随机 session（它们不是本项目的账号身份）

**稳定哈希**：粘滞身份中使用的 URL 安全 SHA-256 截断 hex，输入仅是规范化邮箱。它不需要密钥，应用不同代理时仍保持一致；1024Proxy 将它作为账号稳定 `sid`。它不是代理密码、出口 IP 或每请求随机 token；它不是保密值。
_避免_：账号密码、出口 IP、永久静态 IP、session 随机数、加密密钥

**账号**：由 CPA `auth_index` 定位的 CLIProxyAPI 物理认证身份；邮箱仅用于生成粘滞平台最终 URL 的稳定身份。账号可被应用任意一个已保存代理；同一时刻至多拥有一个账号级 `proxy_url` 和一条账号代理映射。
_避免_：用户、客户、仅由邮箱唯一识别的身份

**账号代理映射**：以 CPA `auth_index` 为键、以保存代理 `proxy_id` 为值的持久化记录。它是 StickyProxy 判断账号已应用哪条代理的唯一来源，不保存邮箱、最终 URL、账号 hash 或会话 SID。
_避免_：从 `proxy_url` 反查代理、账号选择集、最终 URL 缓存

## 账号级代理应用

**应用代理**：操作员从某条已保存代理发起、并对明确选中账号执行的写入操作。插件按平台规则生成最终代理 URL，写入该账号已有的 `proxy_url`；粘滞平台使用账号邮箱，普通代理直接使用保存后的代理地址。应用代理只影响本次选中的账号。
_避免_：切换当前代理、全局代理开关、自动全量同步

**应用目标**：一次“应用代理”操作中被操作员显式选中的 CPA 物理认证账号。应用目标仅驻留在页面请求中，不写入插件状态。
_避免_：账号代理映射、永久选择集

**账号已应用代理**：账号代理映射指向一条当前已保存的代理。页面展示该代理名称；这表示插件最后成功登记的应用关系，不表示上游代理可用。
_避免_：从 URL 推断、当前代理、代理可用

**继承系统代理**：账号 `proxy_url` 为空，因而由 CPA 全局 `proxy-url` 或原有直连决策决定出站方式。
_避免_：未配置账号、应用失败

**其他账号代理**：账号已有非空 `proxy_url`，但没有指向现存代理的账号代理映射。它可能来自人工设置、历史 StickyProxy 配置、已删除代理或其他系统。
_避免_：异常配置（除非操作员明确应用某条代理覆盖它）

**按代理清除**：操作员明确选择一条代理和一组账号后，只清空其中账号代理映射指向该代理的账号 `proxy_url`，并删除这些映射。未映射账号、映射到其他代理的账号和未选择账号不受影响。
_避免_：取消当前代理、全局清理

**清除账号代理**：操作员明确选择账号后清空这些账号的 `proxy_url` 并删除其账号代理映射，使其回退系统代理；不要求它们当前使用 StickyProxy 代理。
_避免_：删除代理、修改全局 proxy-url

## URL 与状态

**基础代理地址**：尚未注入账号会话身份的代理 URL。代理编辑和代理识别以基础代理地址为准。
_避免_：最终代理 URL

**最终代理 URL**：为某个账号按平台规则生成的连接 URL。粘滞平台会加入账号身份；普通代理的最终 URL 就是保存后的代理地址。对 1024Proxy，只替换或注入完整的 `-sid-<stable_hash>` 标记；操作员提供的定位参数和 `-t-...` TTL 文本逐字保留。它仅在 native 插件与 CPA host callback 间使用并写入账号现有 `proxy_url`；页面只显示脱敏预览。
_避免_：原始代理（原始代理可能包含尚未规范化的输入）

**1024Proxy 模板 SID**：基础用户名中可选的完整 `-sid-<id>` 标记。它是操作员提供的模板，不是账号绑定；生成账号或测试 URL 时才由稳定账号 SID 或随机测试 SID 替换。
_避免_：持久化的账号 SID、永久 IP 分配

**不透明 TTL**：1024Proxy 基础用户名中操作员提供的 `-t-...` 文本。StickyProxy 不解释、校验、注入、删除、替换或移动它；无论账号应用还是服务端测试均逐字保留。
_避免_：StickyProxy 的会话时长策略、可由插件校验或重写的字段

**隔离测试 SID**：服务端代理测试临时生成的随机 `test<random>` 1024Proxy `sid`。它与账号稳定 SID 使用不同命名空间，不持久化、不返回页面，并防止测试占用或复用任何账号会话。
_避免_：账号粘滞身份、测试凭据、可展示的 session 值

## 规则

- 代理只接受 HTTP、HTTPS、SOCKS5（输入兼容 `socks://`、`socks5://`、`socks5h://`）；其他协议必须在保存前拒绝。
- Decodo 使用用户名中的 `-session-STABLE_HASH-sessionduration-30`；DataImpulse 使用 `;sessid.STABLE_HASH`；已有标记先移除，避免重复注入。
- Resin 使用正向代理认证的 `Platform.sp_STABLE_HASH` 用户名，其中默认平台标识为 `Default`；密码是 Resin Gateway 的 Proxy Token。Resin 以 `[Platform, Account]` 维护到出口 IP 的粘性租约，而不是通过 `sessid` 请求上游代理会话。
- 1024Proxy session-username 代理要求非空用户名和密码。完整的 `-sid-<id>` 仅在账号或测试 URL 生成时替换/注入；账号使用邮箱派生的稳定 SID，服务端测试使用独立随机 SID。基础用户名中已有的定位参数、模板 SID 和 `-t-...` TTL 均不被规范化或重排，TTL 不受本插件校验或管理。
- 普通代理使用平台标识 `generic`，只接受通用 HTTP、HTTPS、SOCKS5 URL 校验；用户名和密码均可选。它不注入或管理账号 hash、供应商 session、SID 或 TTL，账号和测试都直接使用保存后的代理地址；因此插件不承诺上游出口 IP 粘滞性或测试会话隔离。
- 代理是独立可复用资源，不存在唯一“当前代理”。不同账号可以使用不同代理；再次对同一账号应用另一代理时，仅覆盖该账号原有的 `proxy_url`。
- 插件状态保存代理和账号代理映射；不保存账号选择、邮箱、最终 URL、稳定 hash、会话 SID 或全局代理选择。基础地址按用户决定明文持久化，不引入状态加密密钥。
- 账号已应用代理只由持久化账号代理映射判断，不通过读取、生成、规范化或比较 `proxy_url` 反查；旧 `proxy_url` 不迁移，操作员重新应用代理后才建立映射。
- 无邮箱、未选择或损坏代理配置的账号保留原有系统代理或既有账号代理行为；清除操作只能按操作员明确选择的账号和代理范围执行。
- 代理名称只接受大小写英文字母、数字、短横线和下划线：`[A-Za-z0-9_-]+`。
